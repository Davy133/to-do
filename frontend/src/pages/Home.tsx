import React from 'react';
// import { useAuth } from '../hooks/useAuth';
// import api from '../services/api';
const Home: React.FC = () => {
    return (
        <div>
            <h1>Users</h1>
            {/* <ul>
                {users.map((user) => (
                    <li key={user}></li>
                ))}
            </ul> */}

        </div>
    );
};

export default Home;